const removeElement = (nums, val) => {
    let currIndex = 0

    for (let i = 0; i < nums.length; i++) {
        if (nums[i] != val) {
            nums[currIndex] = nums[i]
            currIndex++
        }
    }

    return currIndex
};
